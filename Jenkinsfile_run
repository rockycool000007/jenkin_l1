pipeline {
    agent { label: "agent2" }
    stages {
        stage("Retrive Artifact") {
            steps {
                copyArtifacts(projectName: 'compile-java-program');
            }
        }  
        stage("Run the code") {
            steps {
                sh "'java' JavaProgram"
            }
        }    
    }
    post {
        always {
            cleanWs()
        }
    }
}